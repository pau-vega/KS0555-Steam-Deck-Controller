pre-commit:
  parallel: true
  commands:
    format:
      run: just format
      stage_fixed: true
    lint:
      run: just lint
    typecheck:
      run: just typecheck

commit-msg:
  commands:
    commitlint:
      run: npx --no -- commitlint --edit "$LEFTHOOK_COMMIT_MSG"
