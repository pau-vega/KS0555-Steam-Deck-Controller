---
phase: 14-steam-deck-on-device-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - flatpak/VALIDATION-CHECKLIST.md
  - flatpak/validation-reports/REPORT-TEMPLATE.md
  - flatpak/validation-logs/.gitkeep
  - flatpak/validation-reports/.gitkeep
  - flatpak/README.md
autonomous: true
requirements:
  - DECK-01
  - DECK-02
  - DECK-03
  - DECK-04
  - VAL-09

must_haves:
  truths:
    - "A reusable VALIDATION-CHECKLIST.md exists with pass/fail steps covering install, Desktop BLE+gamepad, Non-Steam Game, Gaming Mode, offline, and UI validation"
    - "Every checklist step is annotated with the req-ID it validates (DECK-01 through DECK-04, VAL-09)"
    - "The checklist includes exact log capture commands (RUST_LOG=debug redirect, env dump)"
    - "The checklist includes Gaming Mode escalation protocol with black screen workarounds"
    - "The checklist includes failure handling fallbacks for device=input, BLE, install, and black screen"
    - "A report template exists for capturing dated validation results with log snippet slots"
    - "flatpak/README.md documents the validation workflow referencing the checklist"
  artifacts:
    - path: "flatpak/VALIDATION-CHECKLIST.md"
      provides: "Reusable pass/fail validation checklist for Steam Deck"
      contains: "- [ ] PASS / - [ ] FAIL"
    - path: "flatpak/validation-reports/REPORT-TEMPLATE.md"
      provides: "Dated report template for validation runs"
      contains: "## Report"
    - path: "flatpak/validation-logs/.gitkeep"
      provides: "Log directory scaffolding"
    - path: "flatpak/validation-reports/.gitkeep"
      provides: "Reports directory scaffolding"
  key_links:
    - from: "flatpak/VALIDATION-CHECKLIST.md"
      to: "DECK-01 through DECK-04, VAL-09"
      via: "req-ID annotations in parens on each step"
      pattern: "\\(DECK-0[1-4]\\)"
    - from: "flatpak/VALIDATION-CHECKLIST.md"
      to: "flatpak/validation-reports/YYYY-MM-DD-REPORT.md"
      via: "checklist instructs user to copy and fill"
      pattern: "REPORT-TEMPLATE"
    - from: "flatpak/README.md"
      to: "flatpak/VALIDATION-CHECKLIST.md"
      via: "README references the checklist for validation workflow"
      pattern: "VALIDATION-CHECKLIST"
---

<objective>
Create the reusable validation checklist, dated report template, and directory scaffolding for Steam Deck on-device validation. Produces `flatpak/VALIDATION-CHECKLIST.md` (the core deliverable — a comprehensive pass/fail checklist covering all 5 Phase 14 success criteria), `flatpak/validation-reports/REPORT-TEMPLATE.md` (a fillable template), and updated `flatpak/README.md` with validation workflow reference.

Purpose: Phase 14 validates the Flatpak bundle on real Steam Deck hardware. The checklist is the reusable artifact that survives across releases — anyone with a Deck can run it. The report template standardizes how validation results are captured.

Output: Three new files (`VALIDATION-CHECKLIST.md`, `REPORT-TEMPLATE.md`, directory `.gitkeep`s), one modified file (`flatpak/README.md`). No code changes.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/14-steam-deck-on-device-validation/14-CONTEXT.md
@.planning/phases/14-steam-deck-on-device-validation/14-RESEARCH.md
@.planning/research/PITFALLS.md
@flatpak/com.ks0555.robotcontroller.yaml
@flatpak/build.sh
@flatpak/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the reusable validation checklist (VALIDATION-CHECKLIST.md)</name>
  <files>flatpak/VALIDATION-CHECKLIST.md</files>
  <read_first>
    @.planning/phases/14-steam-deck-on-device-validation/14-CONTEXT.md
    @.planning/REQUIREMENTS.md
    @.planning/research/PITFALLS.md
    @flatpak/com.ks0555.robotcontroller.yaml
    @flatpak/build.sh
  </read_first>
  <action>
Create `flatpak/VALIDATION-CHECKLIST.md` — the reusable pass/fail validation checklist for Steam Deck on-device testing. This is a markdown document with `- [ ] PASS / - [ ] FAIL` checkbox format per step, req-ID annotations in parens on every test step, and a precondition section at the top.

**Document structure (markdown):**

```markdown
# Steam Deck On-Device Validation Checklist

> Reusable across releases. Copy this checklist, fill it out during a validation session on a real Steam Deck, and save the result as `flatpak/validation-reports/YYYY-MM-DD-REPORT.md`.

## Preconditions

- [ ] Steam Deck is running SteamOS (record version: _____)
- [ ] Flatpak version is _____ (`flatpak --version`)
- [ ] Bluetooth is enabled on the Steam Deck
- [ ] BT24 robot is powered on, in range, and advertising (blue LED blinking)
- [ ] RobotController-x86_64.flatpak bundle built from commit _____ (`git rev-parse HEAD`)

## 1. Installation (DECK-01)

- [ ] PASS / [ ] FAIL — 1.1. `flatpak install --user RobotController-x86_64.flatpak` completes without error
- [ ] PASS / [ ] FAIL — 1.2. Flathub remote auto-fetches missing runtime `org.freedesktop.Platform//24.08` (if not already cached)
- [ ] PASS / [ ] FAIL — 1.3. `flatpak list | grep com.ks0555.robotcontroller` shows the app installed
- [ ] PASS / [ ] FAIL — 1.4. `flatpak run --command=ls com.ks0555.robotcontroller /app/bin/robot-controller` returns the binary path

## 2. Desktop Mode — BLE + Gamepad (DECK-02)

- [ ] PASS / [ ] FAIL — 2.1. `flatpak run com.ks0555.robotcontroller` opens the app window in Desktop Mode
- [ ] PASS / [ ] FAIL — 2.2. StatusBar shows "Scanning..." or "Disconnected" (not blank or error)
- [ ] PASS / [ ] FAIL — 2.3. App scans and discovers the BT24 robot (StatusBar shows device found)
- [ ] PASS / [ ] FAIL — 2.4. Clicking Connect establishes BLE connection; StatusBar shows "Connected"
- [ ] PASS / [ ] FAIL — 2.5. Steam Deck built-in gamepad registers — ControlPad buttons highlight on press
- [ ] PASS / [ ] FAIL — 2.6. Forward (F) command drives the robot forward (verify robot moves)
- [ ] PASS / [ ] FAIL — 2.7. Back (B) command drives the robot backward (verify robot moves)
- [ ] PASS / [ ] FAIL — 2.8. Left (L) command turns the robot left (verify robot turns)
- [ ] PASS / [ ] FAIL — 2.9. Right (R) command turns the robot right (verify robot turns)
- [ ] PASS / [ ] FAIL — 2.10. Stop (S) command stops the robot (center stick / no input → robot stops)
- [ ] PASS / [ ] FAIL — 2.11. Rapid direction changes (F→B→L→R sequence) — robot responds to each change
  - Latency: _____ (immediate / slight lag / noticeable delay)
- [ ] PASS / [ ] FAIL — 2.12. BLE reconnect: disconnect robot (power off BT24), wait 5s, power on — app reconnects or StatusBar updates correctly
  - Latency: _____ (immediate / slight lag / noticeable delay)

## 3. Non-Steam Game (DECK-03)

- [ ] PASS / [ ] FAIL — 3.1. In Steam Desktop Mode, "Add a Non-Steam Game" picker finds `com.ks0555.robotcontroller.desktop`
- [ ] PASS / [ ] FAIL — 3.2. The `.desktop` file is located at `~/.local/share/flatpak/exports/share/applications/com.ks0555.robotcontroller.desktop`
- [ ] PASS / [ ] FAIL — 3.3. The resulting Steam shortcut uses launch command: `flatpak run com.ks0555.robotcontroller`
- [ ] PASS / [ ] FAIL — 3.4. Launching the shortcut from Steam Desktop Mode opens the app

## 4. Gaming Mode (DECK-04)

- [ ] PASS / [ ] FAIL — 4.1. Switch to Gaming Mode. Launch the Non-Steam Game shortcut.
- [ ] PASS / [ ] FAIL — 4.2. App window renders — NO black screen, NO white screen
  - If black/white screen: try known workarounds (see escalation protocol below)
- [ ] PASS / [ ] FAIL — 4.3. Steam Deck built-in gamepad registers in Gaming Mode — ControlPad buttons respond
- [ ] PASS / [ ] FAIL — 4.4. F/B/L/R/S commands reach the BT24 robot (verify robot moves for each direction)
- [ ] PASS / [ ] FAIL — 4.5. Latency in Gaming Mode: _____ (immediate / slight lag / noticeable delay) — compared to Desktop Mode: _____ (same / worse / better)

### Gaming Mode Escalation Protocol (if black/white screen occurs)

If step 4.2 fails, try these in order. Document which (if any) resolves the issue:

1. [ ] Tried: `flatpak run --env=WEBKIT_DISABLE_DMABUF_RENDERER=1 com.ks0555.robotcontroller` — Result: _____
2. [ ] Tried: X11-only (`flatpak run --nosocket=wayland --socket=fallback-x11 com.ks0555.robotcontroller`) — Result: _____
3. [ ] Tried: Double env vars (both `WEBKIT_DISABLE_COMPOSITING_MODE=1` and `WEBKIT_DISABLE_DMABUF_RENDERER=1`) — Result: _____
4. [ ] Captured: `flatpak run --command=env com.ks0555.robotcontroller` output — file: `validation-logs/YYYY-MM-DD-env.log`
5. [ ] Captured: `flatpak run --command=glxinfo com.ks0555.robotcontroller 2>&1` GPU info — file: _____ (if glxinfo available)

Working combo (if found): _____

## 5. Steam Input Configuration (D-12, D-13)

- [ ] PASS / [ ] FAIL — 5.1. Steam Input ENABLED (default): gamepad events reach the app? (Yes / No)
- [ ] PASS / [ ] FAIL — 5.2. Steam Input set to "Gamepad with Joystick Trackpad" template: gamepad events reach the app? (Yes / No)
- [ ] PASS / [ ] FAIL — 5.3. Steam Input set to "Gamepad" (pass-through): gamepad events reach the app? (Yes / No)

**Recommended Steam Input template:** _____ (document which template from 5.1-5.3 works best)

## 6. Round-Trip (D-14)

- [ ] PASS / [ ] FAIL — 6.1. Desktop Mode → launch app → works ✓
- [ ] PASS / [ ] FAIL — 6.2. Switch to Gaming Mode → launch app → works ✓
- [ ] PASS / [ ] FAIL — 6.3. Switch back to Desktop Mode → launch app → works ✓

## 7. Offline Mode (no BT24 nearby)

- [ ] PASS / [ ] FAIL — 7.1. Launch app with BT24 powered OFF — StatusBar shows "Disconnected" or "Scanning..." (graceful, no crash)
- [ ] PASS / [ ] FAIL — 7.2. Scan times out gracefully (no infinite spinner, no frozen UI)
- [ ] PASS / [ ] FAIL — 7.3. ControlPad buttons are visible but non-functional (expected when no robot connected)

## 8. UI Validation

- [ ] PASS / [ ] FAIL — 8.1. StatusBar shows correct states: Disconnected → Scanning → Connecting → Connected
- [ ] PASS / [ ] FAIL — 8.2. StatusBar re-shows "Disconnected" after robot powers off (BLE auto-disconnect)
- [ ] PASS / [ ] FAIL — 8.3. ControlPad shows all 5 directional buttons (F, B, L, R, S) and they highlight on press
- [ ] PASS / [ ] FAIL — 8.4. App window has correct title "Robot Controller" (not "Tauri App" or blank)
- [ ] PASS / [ ] FAIL — 8.5. Window decorations and sizing work correctly in both Desktop and Gaming Mode

## 9. Edge Cases

- [ ] PASS / [ ] FAIL — 9.1. `--device=input` finish-arg is honored (gamepad works) — if not, test `--device=all` fallback
  - Fallback needed? (Yes / No) — If yes, document Flatpak/SteamOS version: _____
- [ ] PASS / [ ] FAIL — 9.2. BLE works in Gaming Mode (if BLE works in Desktop but not Gaming, document as known limitation per D-16)
  - Known limitation triggered? (Yes / No)
- [ ] PASS / [ ] FAIL — 9.3. Flatpak install failure — if `flatpak install` fails, retry with `--verbose` and capture error: _____
  - Error resolved? (Yes / No) — Fix: _____
- [ ] PASS / [ ] FAIL — 9.4. `flatpak run --command=env com.ks0555.robotcontroller` output includes `WEBKIT_DISABLE_COMPOSITING_MODE=1` (belt-and-suspenders env var is active in sandbox — D-10)
- [ ] PASS / [ ] FAIL — 9.5. `flatpak run --env=RUST_LOG=debug com.ks0555.robotcontroller 2> validation-logs/YYYY-MM-DD-app.log` produces a log file with BLE connect, gamepad-direction events, and ble_send entries (embed key snippets in report per D-08/D-09)

## 10. Summary

- [ ] All DECK-01 criteria pass (installation)
- [ ] All DECK-02 criteria pass (Desktop BLE + gamepad)
- [ ] All DECK-03 criteria pass (Non-Steam Game)
- [ ] All DECK-04 criteria pass (Gaming Mode)
- [ ] VAL-09 satisfied (end-to-end, log artifacts captured)

**Overall:** PASS / FAIL

**Tester:** _____
**Date:** YYYY-MM-DD
**Flatpak build version:** _____ (from `flatpak run --command=sh -c 'echo $FLATPAK_VERSION' com.ks0555.robotcontroller` or build commit hash)
**SteamOS version:** _____ (`cat /etc/os-release | grep VERSION`)
**Steam Deck model:** _____ (LCD / OLED)

## Log Artifacts

- [ ] App log captured: `validation-logs/YYYY-MM-DD-app.log`
- [ ] Env dump captured: `validation-logs/YYYY-MM-DD-env.log`
- [ ] Key log snippets embedded in report (see REPORT-TEMPLATE.md for format)
```

**Implementation rules:**
- Every test step has exactly one `- [ ] PASS / - [ ] FAIL` checkbox
- Every test section header has the req-ID it covers in parens: `## 1. Installation (DECK-01)`
- Preconditions section at top (D-02: Flatpak version, SteamOS version, BT24 powered on, Bluetooth enabled)
- Gaming Mode escalation protocol is inline (not a separate doc) — per D-11
- Steam Input section tests all three configurations and documents recommended template (D-12, D-13)
- Round-trip test is explicit (section 6) — per D-14
- Edge cases section covers `--device=input` fallback (D-15), BLE Gaming Mode limitation (D-16), install retry (D-17), black screen workarounds (D-18), env var verification (D-10), and log capture (D-08/D-09)
- Latency annotations use qualitative scale (D-06): immediate / slight lag / noticeable delay
- No Steam Deck hardware-specific checks beyond app behavior (D-07)
- Checklist is self-contained — a user with a Deck, the .flatpak bundle, and this file can run validation
  </action>
  <verify>
    <automated>grep -c 'PASS / - \[ \] FAIL' flatpak/VALIDATION-CHECKLIST.md | xargs -I{} test {} -ge 30</automated>
  </verify>
  <acceptance_criteria>
    - `flatpak/VALIDATION-CHECKLIST.md` exists and contains at least 30 PASS/FAIL checkbox lines
    - Every section header (## N.) has a req-ID annotation in parens — verify: `grep -c '(DECK-\|VAL-09\|D-[0-9]' flatpak/VALIDATION-CHECKLIST.md` matches expected count
    - Preconditions section includes: "SteamOS", "Flatpak version", "BT24", "Bluetooth" — verify: `grep -c 'SteamOS\|Flatpak version\|BT24\|Bluetooth' flatpak/VALIDATION-CHECKLIST.md | xargs -I{} test {} -ge 4`
    - Gaming Mode escalation protocol is present — verify: `grep -c 'Gaming Mode Escalation' flatpak/VALIDATION-CHECKLIST.md` ≥ 1
    - Steam Input section tests both enabled and pass-through — verify: `grep -c 'Steam Input' flatpak/VALIDATION-CHECKLIST.md` ≥ 3
    - Edge cases section covers --device=input fallback — verify: `grep -c 'device=input' flatpak/VALIDATION-CHECKLIST.md` ≥ 1
    - Log capture command uses exact format from D-08: verify `grep -c 'RUST_LOG=debug.*2>' flatpak/VALIDATION-CHECKLIST.md` ≥ 1
  </acceptance_criteria>
  <done>The reusable validation checklist exists at `flatpak/VALIDATION-CHECKLIST.md` with all 10 sections, req-ID annotations, and pass/fail checkboxes covering all 5 success criteria plus all 18 CONTEXT.md decisions</done>
</task>

<task type="auto">
  <name>Task 2: Create validation report template and directory scaffolding</name>
  <files>flatpak/validation-reports/REPORT-TEMPLATE.md</files>
  <read_first>
    @flatpak/VALIDATION-CHECKLIST.md
    @.planning/phases/14-steam-deck-on-device-validation/14-CONTEXT.md
  </read_first>
  <action>
Create two directories and one template file:

**A. Directory scaffolding:**
```bash
mkdir -p flatpak/validation-reports flatpak/validation-logs
touch flatpak/validation-reports/.gitkeep flatpak/validation-logs/.gitkeep
```

**B. Report template:** Write `flatpak/validation-reports/REPORT-TEMPLATE.md` with this structure:

```markdown
# Validation Report — YYYY-MM-DD

> Copy `flatpak/VALIDATION-CHECKLIST.md` and fill it out, or use this template as a standalone dated report.
> Save as: `flatpak/validation-reports/YYYY-MM-DD-REPORT.md`

## Report Metadata

- **Date:** YYYY-MM-DD
- **Tester:** _____
- **Steam Deck model:** _____ (LCD / OLED)
- **SteamOS version:** _____
- **Flatpak version:** _____
- **Build tested:** commit _____ / version _____
- **BT24 robot:** present / not present
- **Overall result:** PASS / FAIL

## Test Results Summary

| Section | Requirement | Result | Notes |
|---------|-------------|--------|-------|
| 1. Installation | DECK-01 | PASS/FAIL | |
| 2. Desktop BLE + Gamepad | DECK-02 | PASS/FAIL | |
| 3. Non-Steam Game | DECK-03 | PASS/FAIL | |
| 4. Gaming Mode | DECK-04 | PASS/FAIL | |
| 5. Steam Input | D-12/D-13 | N/A (tested) | |
| 6. Round-Trip | D-14 | PASS/FAIL | |
| 7. Offline Mode | (D-05) | PASS/FAIL | |
| 8. UI Validation | (D-05) | PASS/FAIL | |
| 9. Edge Cases | D-15/D-16/D-17/D-18 | See below | |
| 10. Overall | VAL-09 | PASS/FAIL | |

## Detailed Results

_(Copy the full checklist from VALIDATION-CHECKLIST.md and fill in each `[ ]` with `[x]` for tested, annotate PASS/FAIL.)_

## Gaming Mode Notes

- Black screen encountered? (Yes / No)
- Workaround needed? (Yes / No) — If yes, which: _____
- Latency compared to Desktop Mode: same / worse / much worse

## Steam Input Recommendation

- Working template: _____
- Notes: _____

## Key Log Snippets

### BLE connect (from app log)

```
[PASTE relevant log lines showing BLE connection]
```

### Gamepad direction events (from app log)

```
[PASTE relevant log lines showing gamepad-direction events]
```

### Env dump (sandbox verification)

```
[PASTE output of `flatpak run --command=env com.ks0555.robotcontroller`]
```

Verify `WEBKIT_DISABLE_COMPOSITING_MODE=1` is present: (Yes / No)

## Issues Found

| # | Description | Severity | Resolution |
|---|-------------|----------|------------|
| 1 | | | |
| 2 | | | |

## Sign-off

- [ ] All required sections completed
- [ ] Log artifacts saved to `flatpak/validation-logs/YYYY-MM-DD-*.log`
- [ ] This report saved to `flatpak/validation-reports/YYYY-MM-DD-REPORT.md`

---

*Report generated: YYYY-MM-DD*
*Checklist version: from `flatpak/VALIDATION-CHECKLIST.md` commit _____*
```

**Implementation rules:**
- Template references the checklist by name (not inline copy) — per D-04 "not inline fill"
- Template includes dedicated log snippet slots (D-09: "Key log snippets embedded")
- Template includes env dump verification slot (D-10: verify WEBKIT env var)
- Template includes Gaming Mode notes section for recording escalation outcomes
- Template includes Steam Input recommendation field
- Template is fillable — contains `_____` blanks for all dynamic fields
  </action>
  <verify>
    <automated>test -f flatpak/validation-reports/REPORT-TEMPLATE.md && test -f flatpak/validation-reports/.gitkeep && test -f flatpak/validation-logs/.gitkeep</automated>
  </verify>
  <acceptance_criteria>
    - `flatpak/validation-reports/REPORT-TEMPLATE.md` exists — verify: `test -f flatpak/validation-reports/REPORT-TEMPLATE.md`
    - Template contains "Report Metadata" section — verify: `grep -c 'Report Metadata' flatpak/validation-reports/REPORT-TEMPLATE.md` ≥ 1
    - Template contains results summary table with all 5 req-IDs — verify: `grep -c 'DECK-01\|DECK-02\|DECK-03\|DECK-04\|VAL-09' flatpak/validation-reports/REPORT-TEMPLATE.md` ≥ 5
    - Template includes log snippet slots — verify: `grep -c 'Log Snippets\|PASTE' flatpak/validation-reports/REPORT-TEMPLATE.md` ≥ 2
    - Template includes Gaming Mode notes and Steam Input recommendation fields — verify: `grep -c 'Gaming Mode\|Steam Input' flatpak/validation-reports/REPORT-TEMPLATE.md` ≥ 2
    - Directory scaffolding exists — verify: `test -f flatpak/validation-reports/.gitkeep && test -f flatpak/validation-logs/.gitkeep`
  </acceptance_criteria>
  <done>Report template exists at `flatpak/validation-reports/REPORT-TEMPLATE.md`, directories `flatpak/validation-reports/` and `flatpak/validation-logs/` exist with `.gitkeep` files</done>
</task>

<task type="auto">
  <name>Task 3: Update flatpak/README.md with validation section</name>
  <files>flatpak/README.md</files>
  <read_first>
    @flatpak/README.md
    @flatpak/VALIDATION-CHECKLIST.md
  </read_first>
  <action>
Append a new "## Validation" section to `flatpak/README.md` after the existing "Flatpak Manifest" section (after line 86). The README currently ends with the manifest section describing finish-args.

Add this section at the end of the file:

```markdown
## Validation

Before tagging a release, validate the Flatpak bundle on a real Steam Deck using
the on-device validation checklist.

### Quick Start

1. Build the `.flatpak` bundle:
   ```bash
   ./flatpak/build.sh apps/frontend/src-tauri/target/release/bundle/deb/robot-controller_*.deb
   ```

2. Transfer to your Steam Deck (via USB, scp, or KDE Connect).

3. On the Steam Deck:
   ```bash
   flatpak install --user RobotController-x86_64.flatpak
   flatpak run com.ks0555.robotcontroller
   ```

4. Run through the checklist:
   - Copy `flatpak/VALIDATION-CHECKLIST.md` and fill it out
   - Save results as `flatpak/validation-reports/YYYY-MM-DD-REPORT.md`
   - Capture logs: `flatpak run --env=RUST_LOG=debug com.ks0555.robotcontroller 2> flatpak/validation-logs/YYYY-MM-DD-app.log`

5. Verify env: `flatpak run --command=env com.ks0555.robotcontroller | grep WEBKIT`

### Checklist Coverage

The checklist validates:
- **DECK-01:** Sideload install with auto-fetched runtime
- **DECK-02:** Desktop Mode — BLE connect to BT24, gamepad input (F/B/L/R/S)
- **DECK-03:** "Add as Non-Steam Game" — .desktop found, shortcut launches
- **DECK-04:** Gaming Mode — window renders (no black screen), gamepad + BLE work
- **VAL-09:** End-to-end logged session with log artifacts captured

### Validation Artifacts

| File | Purpose |
|------|---------|
| `flatpak/VALIDATION-CHECKLIST.md` | Reusable pass/fail checklist (run on every release) |
| `flatpak/validation-reports/REPORT-TEMPLATE.md` | Template for dated validation reports |
| `flatpak/validation-reports/` | Directory for filled reports (git-ignored except template) |
| `flatpak/validation-logs/` | Directory for captured log files (git-ignored except .gitkeep) |
```

**Rules:**
- Do NOT modify any existing content in README.md — only append
- The new section goes at the very end of the file
- The section uses the same heading level as existing sections (##)
- All commands use backtick code formatting matching existing style
- The table format matches the existing README style (pipe tables)
- Reference the checklist with its actual filename: `flatpak/VALIDATION-CHECKLIST.md`
  </action>
  <verify>
    <automated>grep -c '## Validation' flatpak/README.md | xargs -I{} test {} -ge 1</automated>
  </verify>
  <acceptance_criteria>
    - `flatpak/README.md` contains a "## Validation" section — verify: `grep -c '## Validation' flatpak/README.md` ≥ 1
    - README references the checklist — verify: `grep -c 'VALIDATION-CHECKLIST' flatpak/README.md` ≥ 1
    - README mentions all 5 req-IDs (DECK-01 through DECK-04, VAL-09) — verify: `grep -c 'DECK-01\|DECK-02\|DECK-03\|DECK-04\|VAL-09' flatpak/README.md` ≥ 5
    - README includes the install and run commands — verify: `grep -c 'flatpak install --user' flatpak/README.md` ≥ 1
    - Existing README content is intact (the "Prerequisites", "Building", "Installing and Running", "Regenerating Icons", "Architecture", "Flatpak Manifest" sections are unchanged) — verify: `grep -c '^## Prerequisites' flatpak/README.md` ≥ 1
    - README ends with the new Validation section — verify: `tail -5 flatpak/README.md | grep -c 'Validation\|git-ignored'` ≥ 1
  </acceptance_criteria>
  <done>flatpak/README.md has a complete "Validation" section at the end referencing the checklist, all 5 requirements, and the validation workflow</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer → Validation Checklist | The checklist is a source-of-truth document in version control |
| Tester → Validation Report | Reports contain system info and log snippets that could reveal host details |
| Flatpak sandbox → Host | The validation exercises the sandbox boundary — logs may expose sandbox escape paths if found |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-14-01 | Tampering | VALIDATION-CHECKLIST.md | mitigate | Checklist lives in git version control; modification requires a commit with audit trail. Report template includes a field for the checklist commit hash being validated against. |
| T-14-02 | Information Disclosure | validation-reports/*.md | mitigate | Report template includes a "sanitize before sharing" note; log snippet slots are labeled "[PASTE relevant lines]" not "paste everything". The template does not instruct testers to commit full raw logs. |
| T-14-03 | Repudiation | validation-reports/REPORT-TEMPLATE.md | accept | Reports are for internal developer use during sideload-only distribution. No regulatory compliance requirement. Template captures tester identity and date for traceability. |
| T-14-04 | Information Disclosure | validation-logs/*.log | mitigate | Log directory is git-ignored (only .gitkeep committed). The checklist (D-09) specifies key log snippets in the report, not full raw logs committed to the repo. |
| T-14-05 | Spoofing | Validation report signature | accept | Single developer workflow. No PKI or signing infrastructure. Reports are advisory, not security-critical gates. |
</threat_model>

<verification>
Verify all artifacts exist and are well-formed:

```bash
# All files exist
test -f flatpak/VALIDATION-CHECKLIST.md \
  && test -f flatpak/validation-reports/REPORT-TEMPLATE.md \
  && test -f flatpak/validation-reports/.gitkeep \
  && test -f flatpak/validation-logs/.gitkeep \
  && echo "✓ All 4 validation files exist"

# Checklist has at least 30 PASS/FAIL lines
PASSFAIL=$(grep -c 'PASS / - \[ \] FAIL' flatpak/VALIDATION-CHECKLIST.md 2>/dev/null || echo 0)
test "$PASSFAIL" -ge 30 && echo "✓ Checklist has $PASSFAIL PASS/FAIL checkboxes" || echo "✗ Only $PASSFAIL checkboxes (< 30)"

# All 5 req-IDs appear in the checklist
for req in DECK-01 DECK-02 DECK-03 DECK-04 VAL-09; do
  grep -q "$req" flatpak/VALIDATION-CHECKLIST.md && echo "✓ $req covered" || echo "✗ $req MISSING"
done

# Report template references all 5 req-IDs
for req in DECK-01 DECK-02 DECK-03 DECK-04 VAL-09; do
  grep -q "$req" flatpak/validation-reports/REPORT-TEMPLATE.md && echo "✓ $req in template" || echo "✗ $req MISSING from template"
done

# README has validation section
grep -q '## Validation' flatpak/README.md && echo "✓ README updated" || echo "✗ README missing validation section"
```
</verification>

<success_criteria>
- [ ] `flatpak/VALIDATION-CHECKLIST.md` created with 10 sections, ≥30 PASS/FAIL checkboxes, req-ID annotations
- [ ] Checklist covers: install (DECK-01), Desktop BLE+gamepad (DECK-02), Non-Steam Game (DECK-03), Gaming Mode (DECK-04), Steam Input, round-trip, offline, UI validation, edge cases
- [ ] Checklist incorporates all 18 CONTEXT.md decisions (D-01 through D-18)
- [ ] `flatpak/validation-reports/REPORT-TEMPLATE.md` created with metadata, results summary table, log snippet slots, Gaming Mode notes, Steam Input field
- [ ] `flatpak/validation-reports/.gitkeep` and `flatpak/validation-logs/.gitkeep` exist
- [ ] `flatpak/README.md` updated with "## Validation" section referencing the checklist and all 5 requirements
- [ ] No application code modified — only documentation files in `flatpak/`
- [ ] Threat model registered with 5 STRIDE threats, all with dispositions
</success_criteria>

<output>
After completion, create `.planning/phases/14-steam-deck-on-device-validation/14-01-SUMMARY.md`
</output>
