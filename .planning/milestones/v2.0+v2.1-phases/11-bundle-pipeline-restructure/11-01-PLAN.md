---
phase: 11-bundle-pipeline-restructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/frontend/src-tauri/tauri.conf.json
autonomous: true
requirements:
  - PKG-01

must_haves:
  truths:
    - "`bundle.targets` is `[\"deb\"]` — no `appimage` target remains"
    - "Deb metadata block exists with `\"depends\": []`"
    - "AppImage-specific `linux.appimage` config block is removed"
    - "`cargo tauri build --bundles deb` succeeds from `apps/frontend/src-tauri`"
  artifacts:
    - path: "apps/frontend/src-tauri/tauri.conf.json"
      provides: "Tauri bundle configuration"
      contains: "\"targets\": [\"deb\"]"
      must_not_contain: "appimage"
  key_links:
    - from: "tauri.conf.json bundle.targets"
      to: "flatpak-builder input artifact"
      via: "cargo tauri build --bundles deb"
      pattern: "the .deb produced by this config is consumed by the Phase 12 manifest"
---

<objective>
Switch Tauri bundle target from AppImage to `.deb` in tauri.conf.json, add deb metadata, remove AppImage-specific config.

Purpose: The `.deb` is the input artifact for flatpak-builder (Phase 12). Tauri v2 has no `flatpak` bundle target — deb is the correct intermediate format per STACK.md and PITFALLS.md #1.
Output: Updated `tauri.conf.json` producing a valid `.deb` when `cargo tauri build --bundles deb` is run.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-bundle-pipeline-restructure/11-CONTEXT.md
@.planning/phases/11-bundle-pipeline-restructure/11-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Switch tauri.conf.json bundle target to deb</name>
  <files>apps/frontend/src-tauri/tauri.conf.json</files>
  <read_first>
- apps/frontend/src-tauri/tauri.conf.json — current state of the bundle config
- .planning/phases/11-bundle-pipeline-restructure/11-CONTEXT.md — D-11 (deb metadata), the agent's Discretion (config fields)
- .planning/research/PITFALLS.md — Pitfall #1 (no flatpak target, deb is the correct intermediate)
  </read_first>
  <action>
In `apps/frontend/src-tauri/tauri.conf.json`, make the following changes (per D-11 and PKG-01):

1. **Line 28:** Change `"targets": ["appimage"]` → `"targets": ["deb"]`

2. **Lines 30-34 (the `"linux"` block):** Replace the existing `"linux": { "appimage": { "bundleMediaFramework": false } }` block with deb-specific config:
```json
"linux": {
  "deb": {
    "depends": []
  }
}
```
The `"depends": []` array is correct — Tauri statically bundles libwebkit2gtk-4.1, so no external system dependencies are needed.

3. **The `"macOS"` block (lines 35-37):** Remove entirely. macOS bundling is out of scope for v2.1 (per D-03); the `build-macos` CI job is being dropped in Plan 02.

**Do NOT modify:** `productName`, `version`, `identifier`, `build` section, `app` section, `security.csp` — only the `bundle` block changes.

**Final bundle section should be:**
```json
"bundle": {
  "active": true,
  "targets": ["deb"],
  "icon": ["icons/icon.png"],
  "linux": {
    "deb": {
      "depends": []
    }
  }
}
```
  </action>
  <verify>
    <automated>grep '"targets": \["deb"\]' apps/frontend/src-tauri/tauri.conf.json && grep '"depends": \[\]' apps/frontend/src-tauri/tauri.conf.json && ! grep -i '"appimage"' apps/frontend/src-tauri/tauri.conf.json</automated>
  </verify>
  <acceptance_criteria>
- `grep '"targets": \["deb"\]' apps/frontend/src-tauri/tauri.conf.json` exits 0 (line 28 or equivalent)
- `grep '"depends": \[\]' apps/frontend/src-tauri/tauri.conf.json` exits 0 (new deb metadata block present)
- `grep -i '"appimage"' apps/frontend/src-tauri/tauri.conf.json` exits 1 (no appimage refs remain)
- `grep '"macOS"' apps/frontend/src-tauri/tauri.conf.json` exits 1 (macOS block removed) — skip if macOS block was already absent or is just `"macOS": {}`
- `grep '"productName": "Robot Controller"' apps/frontend/src-tauri/tauri.conf.json` exits 0 (identity fields preserved)
- `grep '"identifier": "com.ks0555.robotcontroller"' apps/frontend/src-tauri/tauri.conf.json` exits 0 (identity fields preserved)
- File is valid JSON: `python3 -c "import json; json.load(open('apps/frontend/src-tauri/tauri.conf.json'))"` exits 0
  </acceptance_criteria>
  <done>tauri.conf.json bundle.targets is ["deb"] with deb metadata, no appimage or macOS config remains</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → tauri.conf.json | Config file edited locally, no runtime trust boundary crossed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | tauri.conf.json | accept | Config-only phase — no runtime attack surface. Config is version-controlled and reviewed via PR. |
</threat_model>

<verification>
After task completion, confirm:
1. `grep '"targets": \["deb"\]' apps/frontend/src-tauri/tauri.conf.json` returns the expected line
2. No string `appimage` (case-insensitive) appears in the file
3. The file is valid JSON
4. `pnpm typecheck` passes (no TypeScript config consumers broken)
</verification>

<success_criteria>
- [ ] `bundle.targets` is `["deb"]` — verified by grep
- [ ] Deb metadata `"depends": []` present
- [ ] No `appimage` string in tauri.conf.json
- [ ] macOS bundle config removed
- [ ] PKG-01 satisfied
</success_criteria>

<output>
After completion, create `.planning/phases/11-bundle-pipeline-restructure/11-01-SUMMARY.md`
</output>
