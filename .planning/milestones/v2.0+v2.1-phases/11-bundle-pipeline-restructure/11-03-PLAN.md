---
phase: 11-bundle-pipeline-restructure
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - build-steamdeck.sh
  - .planning/PROJECT.md
  - .planning/ROADMAP.md
autonomous: true
requirements:
  - PKG-04
  - PKG-02

must_haves:
  truths:
    - "build-steamdeck.sh is deleted from the repository"
    - "PROJECT.md Key Decisions table records the Flatpak runtime: `org.freedesktop.Platform//24.08` with GL extension"
    - "ROADMAP.md Phase 11 success criteria updated with the locked runtime identifier"
    - "No references to the custom tauri-cli fork remain in PROJECT.md"
  artifacts:
    - path: "build-steamdeck.sh"
      provides: "N/A — must NOT exist"
      min_lines: 0
    - path: ".planning/PROJECT.md"
      provides: "Project decisions including Flatpak runtime lock-in"
      contains: "org.freedesktop.Platform//24.08"
  key_links:
    - from: "PROJECT.md Key Decisions"
      to: "Phase 12 manifest (PKG-05)"
      via: "runtime identifier string"
      pattern: "the runtime string in PROJECT.md is the single source of truth consumed by Phase 12's manifest authoring"
</objective>

<objective>
Delete the redundant `build-steamdeck.sh` local build script, lock in the Flatpak runtime decision in PROJECT.md and ROADMAP.md, and remove stale AppImage references from project docs.

Purpose: The build-steamdeck.sh script installs the custom tauri-cli fork and builds AppImage — both being removed in this phase. The Flatpak runtime choice (`org.freedesktop.Platform//24.08` per D-07, D-08) must be recorded as the single source of truth for all downstream phases. Stale AppImage references in project docs are cleaned up.
Output: Deleted build script, updated PROJECT.md and ROADMAP.md with locked runtime decision.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/PROJECT.md
@.planning/phases/11-bundle-pipeline-restructure/11-CONTEXT.md
@.planning/phases/11-bundle-pipeline-restructure/11-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete build-steamdeck.sh</name>
  <files>build-steamdeck.sh</files>
  <read_first>
- build-steamdeck.sh — current contents (88-line bash script)
- .planning/phases/11-bundle-pipeline-restructure/11-CONTEXT.md — D-01 (delete build-steamdeck.sh)
  </read_first>
  <action>
Delete `build-steamdeck.sh` from the repository root. Per D-01: "Remove build-steamdeck.sh entirely — CI handles all builds, local script is redundant."

This script:
- Installs the `feat/truly-portable-appimage` custom tauri-cli fork (being dropped in Plan 02)
- Builds AppImage (target being switched to deb in Plan 01)
- Contains SteamOS-specific read-only filesystem workarounds (no longer needed — Flatpak runs without root)

No replacement script is needed. CI via `.github/workflows/build.yml` is the single build path.

Verify no other files reference `build-steamdeck.sh` before deleting:
```bash
grep -r "build-steamdeck" . --include="*.md" --include="*.yml" --include="*.json" --include="*.sh" 2>/dev/null | grep -v "11-CONTEXT\|11-PLAN\|11-RESEARCH\|.planning\|node_modules"
```
If any references found outside .planning/, update them to point to CI or remove the reference.
  </action>
  <verify>
    <automated>test ! -f build-steamdeck.sh</automated>
  </verify>
  <acceptance_criteria>
- `test -f build-steamdeck.sh` exits 1 (file does not exist)
- `grep -r "build-steamdeck" . --include="*.md" --include="*.yml" --include="*.json" 2>/dev/null | grep -v ".planning/" | grep -v "node_modules"` returns nothing (no stale references in project files)
  </acceptance_criteria>
  <done>build-steamdeck.sh deleted, no stale references remain in project files</done>
</task>

<task type="auto">
  <name>Task 2: Lock Flatpak runtime in PROJECT.md and clean up ROADMAP.md</name>
  <files>.planning/PROJECT.md, .planning/ROADMAP.md</files>
  <read_first>
- .planning/PROJECT.md — current Key Decisions table, Active requirements, and Constraints sections
- .planning/ROADMAP.md — Phase 11 success criteria, Phase 12 runtime references
- .planning/phases/11-bundle-pipeline-restructure/11-CONTEXT.md — D-07 (runtime: freedesktop 24.08), D-08 (GL extension), the agent's Discretion
- .planning/research/STACK.md — § "Recommended Stack" (runtime details, rationale)
  </read_first>
  <action>
Two files to update:

### 1. `.planning/PROJECT.md` — Key Decisions Table

Find the existing Key Decisions table and add a new row for the Flatpak runtime decision. If a "Flatpak runtime" placeholder row exists, replace it with the locked value. Add the GL extension as a separate row or combine.

The decision text to record:
```
| Flatpak runtime | `org.freedesktop.Platform//24.08` with SDK `org.freedesktop.Sdk//24.08` | Smaller (~300 MB), no GNOME deps, ships WebKitGTK-6 for Tauri v2. Extension: `org.freedesktop.Platform.GL.default` for GPU rendering. EOL Aug 2027. | Phase 11 |
```

Also update the "Active" requirements section: if it mentions "AppImage" anywhere, update to reflect the deb intermediate + Flatpak final path. Specifically, change:
- "Flatpak bundle build target replaces AppImage in CI" → "Deb bundle target feeds flatpak-builder; Flatpak bundle is the release artifact"

### 2. `.planning/ROADMAP.md` — Phase 11 Success Criteria

Update Phase 11's success criterion #4 to lock in the runtime string explicitly:
- Current: "Flatpak runtime decision is committed to PROJECT.md Key Decisions table — either `org.freedesktop.Platform//24.08` (recommended) or `org.gnome.Platform//46` (fallback) — and the same string appears in the Phase 12 manifest plan"
- Replace with: "Flatpak runtime `org.freedesktop.Platform//24.08` with SDK `org.freedesktop.Sdk//24.08` and extension `org.freedesktop.Platform.GL.default` is committed to PROJECT.md Key Decisions table and referenced in Phase 12 manifest plan"

Also check Phase 12's success criterion #1 for the runtime placeholder and update it to reference the locked runtime explicitly instead of "from PKG-04" (if the text is ambiguous).

### 3. `.planning/ROADMAP.md` — Cross-Reference Check

Verify these strings do NOT appear in any ROADMAP.md sections for Phases 11-16:
- `org.gnome.Platform//46` (runtime decision is locked — no fallback ambiguity)
- `feat/truly-portable-appimage` (fork is dropped)

If found in phase descriptions, simplify to reference the locked runtime or remove fork mentions.
  </action>
  <verify>
    <automated>grep -q "org.freedesktop.Platform//24.08" .planning/PROJECT.md && grep -q "org.freedesktop.Sdk//24.08" .planning/PROJECT.md && grep -q "org.freedesktop.Platform.GL.default" .planning/PROJECT.md && ! grep -q "org.gnome.Platform" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
- `grep 'org.freedesktop.Platform//24.08' .planning/PROJECT.md` exits 0 (runtime locked in PROJECT.md)
- `grep 'org.freedesktop.Sdk//24.08' .planning/PROJECT.md` exits 0 (SDK locked in PROJECT.md)
- `grep 'org.freedesktop.Platform.GL.default' .planning/PROJECT.md` exits 0 (GL extension recorded)
- `grep 'org.gnome.Platform' .planning/ROADMAP.md` exits 1 (no GNOME fallback ambiguity in ROADMAP)
- `grep 'feat/truly-portable-appimage' .planning/ROADMAP.md` exits 1 (no fork refs in ROADMAP)
- `grep 'PKG-04' .planning/ROADMAP.md` still appears (requirement mapping preserved)
- Phase 11 success criterion #4 in ROADMAP.md references the locked runtime string, not the "either/or" text
  </acceptance_criteria>
  <done>Flatpak runtime locked in PROJECT.md Key Decisions, ROADMAP.md success criteria updated, no stale fork/runtime refs remain</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local filesystem → git repo | build-steamdeck.sh deletion is a local filesystem operation, verified by git |
| PROJECT.md editor → downstream consumers | Runtime string is the single source of truth for Phases 12-16 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | PROJECT.md runtime decision | mitigate | PROJECT.md is version-controlled. The runtime string `org.freedesktop.Platform//24.08` is a well-known identifier — typos would cause manifest validation failures in Phase 12. Grep verification ensures the exact string is present. |
</threat_model>

<verification>
After task completion, confirm:
1. `test ! -f build-steamdeck.sh` — script deleted
2. `grep "org.freedesktop.Platform//24.08" .planning/PROJECT.md` — runtime recorded
3. `grep "org.freedesktop.Platform.GL.default" .planning/PROJECT.md` — GL extension recorded
4. No `org.gnome.Platform` or `feat/truly-portable-appimage` in ROADMAP.md
5. `pnpm typecheck && pnpm lint` — no regressions from doc-only changes
</verification>

<success_criteria>
- [ ] build-steamdeck.sh deleted (D-01)
- [ ] Flatpak runtime locked in PROJECT.md Key Decisions (D-07, D-08, PKG-04)
- [ ] ROADMAP.md Phase 11 criterion #4 uses locked runtime string, not "either/or"
- [ ] No GNOME runtime or custom fork refs in ROADMAP.md
- [ ] PKG-04 satisfied
- [ ] Phase 12 manifest plan can reference the runtime string from PROJECT.md
</success_criteria>

<output>
After completion, create `.planning/phases/11-bundle-pipeline-restructure/11-03-SUMMARY.md`
</output>
