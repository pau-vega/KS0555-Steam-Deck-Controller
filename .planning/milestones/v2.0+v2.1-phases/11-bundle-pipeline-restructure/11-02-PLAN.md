---
phase: 11-bundle-pipeline-restructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/build.yml
autonomous: true
requirements:
  - PKG-02
  - PKG-03

must_haves:
  truths:
    - "build.yml has exactly 1 job: `build-x64` (no arm64, no macos)"
    - "build-x64 uses stock `cargo install tauri-cli` (no custom fork, no `--git` flag)"
    - "Build command is `cargo tauri build --bundles deb` (no `--config` JSON override)"
    - "No upload-artifact, softprops/action-gh-release, or artifact renaming steps"
    - "Trigger pattern preserved: tag push `v*` + `workflow_dispatch`"
  artifacts:
    - path: ".github/workflows/build.yml"
      provides: "Single-job CI pipeline building deb with stock tauri-cli"
      contains: "cargo tauri build --bundles deb"
      must_not_contain: "feat/truly-portable-appimage"
      must_not_contain: "build-arm64"
      must_not_contain: "build-macos"
  key_links:
    - from: "build.yml build-x64 job"
      to: "apps/frontend/src-tauri/tauri.conf.json"
      via: "cargo tauri build --bundles deb"
      pattern: "CI build command reads bundle.targets from tauri.conf.json"
</objective>

<objective>
Rewrite `.github/workflows/build.yml` to a single `build-x64` job that builds a `.deb` with stock tauri-cli, removes arm64 and macOS jobs, and drops all artifact upload/release steps.

Purpose: The deb is an intermediate artifact (consumed by flatpak-builder in Phase 12/15), not a release asset. Arm64 and macOS are out of scope for v2.1 Flatpak (Steam Deck is x86_64 only). The custom tauri-cli fork is only needed for AppImage sharun format — deb works with stock CLI.
Output: Simplified `build.yml` with one job verifying deb build.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-bundle-pipeline-restructure/11-CONTEXT.md
@.planning/phases/11-bundle-pipeline-restructure/11-RESEARCH.md
@.planning/research/STACK.md
@.planning/research/PITFALLS.md
@.planning/phases/10-build-and-test-on-steamos/10-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite build.yml to single deb job with stock tauri-cli</name>
  <files>.github/workflows/build.yml</files>
  <read_first>
- .github/workflows/build.yml — current 3-job AppImage CI workflow (174 lines)
- .planning/phases/11-bundle-pipeline-restructure/11-CONTEXT.md — D-02 through D-06 (all CI decisions)
- .planning/phases/10-build-and-test-on-steamos/10-CONTEXT.md — D-02 (trigger pattern: tag push + workflow_dispatch — preserved)
- .planning/research/STACK.md — § "Integration with Existing CI" and § "What NOT to Use" (stock CLI vs fork)
  </read_first>
  <action>
Rewrite `.github/workflows/build.yml` completely. The file will have exactly ONE job (`build-x64`), with these specifics:

**Workflow header (preserve with rename):**
```yaml
name: Build Tauri deb
```
Change from `Build Tauri AppImage` to `Build Tauri deb` (per the agent's Discretion in CONTEXT.md).

**Trigger (preserved per Phase 10 D-02):**
```yaml
on:
  push:
    tags: ["v*"]
  workflow_dispatch:
```

**Permissions:**
```yaml
permissions:
  contents: read
```
Change from `contents: write` — no more release uploads (per D-06).

**Single job `build-x64`:**
```yaml
jobs:
  build-x64:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4

      - uses: pnpm/action-setup@v5

      - uses: actions/setup-node@v4
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - uses: dtolnay/rust-toolchain@stable

      - name: install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libwebkit2gtk-4.1-dev libayatana-appindicator3-dev librsvg2-dev libudev-dev patchelf

      - name: install stock tauri-cli
        run: cargo install tauri-cli

      - name: install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: build frontend
        run: pnpm build

      - name: build deb
        run: cargo tauri build --bundles deb
        working-directory: apps/frontend/src-tauri
```

**Key changes from current build.yml:**
1. Line 1: `name:` from `Build Tauri AppImage` → `Build Tauri deb`
2. Line 9: `permissions: contents: write` → `permissions: contents: read`
3. Line 31-32: Replace `cargo install tauri-cli --git https://github.com/tauri-apps/tauri --branch feat/truly-portable-appimage` → `cargo install tauri-cli` (stock, per D-04, PKG-02)
4. Lines 40-44: Replace the `cargo tauri build --config '{"bundle":{"linux":{"appimage":{"useNewFormat":true}}}}'` step → `cargo tauri build --bundles deb` (per PKG-01)
5. Lines 46-65: Remove ALL post-build steps: artifact rename, upload-artifact, softprops/action-gh-release. Deb is intermediate only (per D-06).
6. Lines 67-124: Remove `build-arm64` job entirely (per D-02, PKG-02)
7. Lines 125-174: Remove `build-macos` job entirely (per D-03, PKG-02)

**Do NOT remove:** system deps install (apt-get), pnpm setup, node setup, rust toolchain, pnpm install, pnpm build — these are all needed for the deb build.

**No new job added** — the `build-flatpak` job goes in Phase 15 (per D-09).
  </action>
  <verify>
    <automated>grep -c "^  build-x64:" .github/workflows/build.yml | grep -q "^1$" && grep -c "^  build-arm64:" .github/workflows/build.yml | grep -q "^0$" && grep -c "^  build-macos:" .github/workflows/build.yml | grep -q "^0$" && grep -c "feat/truly-portable-appimage" .github/workflows/build.yml | grep -q "^0$" && grep -c "cargo tauri build --bundles deb" .github/workflows/build.yml | grep -q "^1$" && grep -c "upload-artifact" .github/workflows/build.yml | grep -q "^0$" && grep -c "action-gh-release" .github/workflows/build.yml | grep -q "^0$"</automated>
  </verify>
  <acceptance_criteria>
- `grep -c '^  build-x64:' .github/workflows/build.yml` returns exactly `1` (one job definition)
- `grep -c '^  build-arm64:' .github/workflows/build.yml` returns `0` (arm64 job removed)
- `grep -c '^  build-macos:' .github/workflows/build.yml` returns `0` (macOS job removed)
- `grep -c 'feat/truly-portable-appimage' .github/workflows/build.yml` returns `0` (no custom fork)
- `grep -c 'cargo tauri build --bundles deb' .github/workflows/build.yml` returns `1` (deb build command)
- `grep -c 'upload-artifact' .github/workflows/build.yml` returns `0` (no artifact upload)
- `grep -c 'action-gh-release' .github/workflows/build.yml` returns `0` (no release step)
- `grep 'cargo install tauri-cli' .github/workflows/build.yml` does NOT contain `--git` (stock CLI only)
- `grep 'Build Tauri deb' .github/workflows/build.yml` exits 0 (name updated)
- `grep 'contents: read' .github/workflows/build.yml` exits 0 (permissions reduced)
- Trigger: `grep 'tags: \["v\*"\]' .github/workflows/build.yml` exits 0 AND `grep 'workflow_dispatch' .github/workflows/build.yml` exits 0
- File is valid YAML: `python3 -c "import yaml; yaml.safe_load(open('.github/workflows/build.yml'))"` exits 0 (or skip if PyYAML not installed)
  </acceptance_criteria>
  <done>build.yml has exactly 1 job using stock tauri-cli to build deb, no fork refs, no arm64/macos, no artifact uploads</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub repository → CI runner | CI workflow definition stored in repo, executed on GitHub-hosted runner |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-02 | Tampering | build.yml CI workflow | mitigate | Workflow file is version-controlled in the repo. `permissions: contents: read` prevents the CI job from modifying the repo or releases. `cargo install tauri-cli` pulls from crates.io (no untrusted git sources). PR review required before merge. |
</threat_model>

<verification>
After task completion, confirm:
1. `grep -c '^  build-x64:' .github/workflows/build.yml` → `1`
2. `grep -c '^  build-arm64:' .github/workflows/build.yml` → `0`
3. `grep -c '^  build-macos:' .github/workflows/build.yml` → `0`
4. `grep -c 'feat/truly-portable-appimage' .github/workflows/build.yml` → `0`
5. `grep -c 'cargo tauri build --bundles deb' .github/workflows/build.yml` → `1`
6. `grep -c 'upload-artifact\|action-gh-release' .github/workflows/build.yml` → `0`
7. `pnpm lint` passes (ESLint — unrelated to YAML but project pre-commit hook)
</verification>

<success_criteria>
- [ ] build.yml has exactly 1 job: `build-x64`
- [ ] No custom tauri-cli fork (`feat/truly-portable-appimage` absent)
- [ ] Build command is `cargo tauri build --bundles deb`
- [ ] No artifact upload or release steps
- [ ] Trigger preserved: tag push `v*` + `workflow_dispatch`
- [ ] PKG-02 satisfied (fork dropped)
- [ ] PKG-03 verification surface established (build succeeds → deb exists)
</success_criteria>

<output>
After completion, create `.planning/phases/11-bundle-pipeline-restructure/11-02-SUMMARY.md`
</output>
