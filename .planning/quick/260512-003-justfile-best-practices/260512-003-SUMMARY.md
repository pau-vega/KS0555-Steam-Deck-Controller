---
status: complete
---

# Summary: Justfile Best Practices for Monorepo

## Changes Made

1. **Fixed default recipe bug**: Removed `[default]` attribute from `phoenix` recipe. Previously running `just` without arguments would execute `phoenix` (nuke + reinstall dependencies), which is destructive and unexpected. Now it correctly shows the recipe list.

2. **Added `set positional-arguments := true`**: Enables positional argument support (`$1`, `$2`, etc.) in shell commands for future recipes that need it.

3. **Added aliases for common commands**:
   - `c` → `check` (lint + typecheck + test)
   - `f` → `format` (prettier format)
   - `l` → `lint` (eslint)
   - `t` → `test` (vitest)

## Verification

- `just` (no args) → shows recipe list ✓
- `just --list` → shows aliases in output ✓
- `just l` → runs lint ✓
- `just t` → runs tests (183 passed) ✓
- `just c` → runs full suite (lint + typecheck + test) ✓
- `just lint` → works ✓
- `just check` → works ✓
