# Plan: Justfile Best Practices for Monorepo

**Goal:** Apply justfile best practices to the project's Justfile, fix default recipe bug, and verify all recipes still work.

## Tasks

### Task 1: Fix default recipe bug

- **Action:** Remove `[default]` attribute from `phoenix` recipe
- **Files:** `Justfile`
- **Verify:** `just` (no args) shows recipe list, not destructive nuke+install

### Task 2: Add aliases for common commands

- **Action:** Add `c := check`, `f := format`, `l := lint`, `t := test` aliases
- **Files:** `Justfile`
- **Verify:** `just l` runs lint, `just t` runs test, `just c` runs check, `just f` runs format

### Task 3: Set positional-arguments for better arg handling

- **Action:** Add `set positional-arguments := true`
- **Files:** `Justfile`
- **Verify:** Recipes with variadic args work correctly

### Task 4: Verify all just scripts work

- **Action:** Run `just --list`, `just dev` (dry-run), `just lint`, `just check`
- **Verify:** No errors
