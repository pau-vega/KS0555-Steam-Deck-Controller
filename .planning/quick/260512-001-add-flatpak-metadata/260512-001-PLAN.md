---
phase: quick
plan: "001"
type: execute
wave: 1
depends_on: []
files_modified:
  - flatpak/com.ks0555.robotcontroller.metainfo.xml
autonomous: true
requirements: []
must_haves:
  truths:
    - "Flatpak metainfo.xml contains keywords for searchability"
    - "Flatpak metainfo.xml contains screenshots section"
    - "Flatpak metainfo.xml contains content rating"
    - "Flatpak metainfo.xml contains architecture metadata"
  artifacts:
    - path: flatpak/com.ks0555.robotcontroller.metainfo.xml
      provides: "AppStream metadata for Flatpak package"
  key_links:
    - from: flatpak/com.ks0555.robotcontroller.metainfo.xml
      to: Flatpak listing
      via: appstream-compose validation
---

<objective>
Add enhanced AppStream metadata to the Flatpak metainfo.xml for better discoverability and compliance.

Purpose: Improve Flatpak package quality with search keywords, screenshots placeholder, content rating, and architecture info.
Output: Updated metainfo.xml with comprehensive metadata.
</objective>

<execution_context>
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/pauvelascogarrofe/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@flatpak/com.ks0555.robotcontroller.metainfo.xml
@flatpak/com.ks0555.robotcontroller.yaml

Current version: 0.1.21 (from Cargo.toml)
</context>

<tasks>

<task type="auto">
  <name>Add enhanced AppStream metadata to metainfo.xml</name>
  <files>flatpak/com.ks0555.robotcontroller.metainfo.xml</files>
  <action>
Add the following metadata elements to the metainfo.xml:

1. **Keywords** - Add `<keywords>` section with: robot, controller, steam-deck, gamepad, bluetooth, ble, robotics
2. **Screenshots** - Add `<screenshots>` section with placeholder (can be filled later with actual UI screenshots)
3. **Content Rating** - Add `<content_rating type="os-specific">` with basic rating (unchanged - all ages)
4. **Architecture** - Add `<provides>` with the binary `robot-controller`
5. **supports** - Add `<supports>` for x86_64 platform

Keep all existing content (releases, description, etc.) intact.
  </action>
  <verify>grep -c "keywords\|screenshots\|content_rating\|provides\|supports" flatpak/com.ks0555.robotcontroller.metainfo.xml</verify>
  <done>Metainfo.xml contains keywords, screenshots section, content rating, provides, and supports elements</done>
</task>

</tasks>

<verification>
[Quick validation - no full verification needed for metadata-only task]
</verification>

<success_criteria>
- Keywords section present with relevant search terms
- Screenshots section present (placeholder OK)
- Content rating present
- Provides binary element present
- Supports x86_64 element present
- XML validates (no broken tags)
</success_criteria>

<output>
After completion, create `.planning/quick/260512-001-add-flatpak-metadata/260512-001-SUMMARY.md`
</output>