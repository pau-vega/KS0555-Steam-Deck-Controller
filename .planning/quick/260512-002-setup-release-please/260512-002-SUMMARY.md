---
status: complete
---

# Quick Task 260512-002: Setup release-please and reset to 0.0.1

## Completed

1. **Deleted all releases (v0.1.1–v0.1.21)** via `gh release delete`
2. **Deleted all remote v0.1.x tags** via `git push origin --delete`
3. **Deleted all local v0.1.x tags** via `git tag -d`
4. **Created `.github/release-please-config.json`** — configures release-please with `release-type: node`, conventional commits sections, initial version 0.0.1
5. **Created `.release-please-manifest.json`** — tracks version at `{".": "0.0.1"}`
6. **Created `.github/workflows/release-please.yml`** — CI workflow that runs release-please on push to main
7. **Updated `package.json`** — version reset to `"0.0.1"`

## Reference

Based on release-please setup in https://github.com/pau-vega/nordvpn-action (`.github/release-please-config.json`, `.release-please-manifest.json`, `.github/workflows/release-please.yml`), simplified for single-package project (no multi-component floating tags).
