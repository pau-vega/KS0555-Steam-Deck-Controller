---
status: complete
---

# Summary: Switch release-please to PAT

## Done
- Researched release-please-action docs for PAT conventions
- Changed `token: ${{ secrets.GITHUB_TOKEN }}` → `token: ${{ secrets.RELEASE_PLEASE_PAT }}` in `.github/workflows/release-please.yml`
- Created PR #43: `fix/release-please-pat`

## Remaining (manual)
1. Create classic PAT with `repo` scope at https://github.com/settings/tokens
2. Add as `RELEASE_PLEASE_PAT` secret in repo Settings → Secrets and variables → Actions
3. Merge PR #43

## Impact
Once the secret exists and PR #43 is merged, CI and Build workflows will run as status checks on future release-please PRs.
