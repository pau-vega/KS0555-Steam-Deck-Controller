# Plan: Switch release-please to PAT

## Task 1: Update token reference in release-please.yml

- **file**: `.github/workflows/release-please.yml`
- **action**: Change `token: ${{ secrets.GITHUB_TOKEN }}` to `token: ${{ secrets.RELEASE_PLEASE_PAT }}`
- **verify**: Confirm the diff shows only the token value changed
- **done**: File updated, ready for PR

## Notes

- User must create a classic PAT with `repo` scope in GitHub Settings → Developer settings → Personal access tokens → Tokens (classic)
- User must save the PAT as `RELEASE_PLEASE_PAT` in repo Settings → Secrets and variables → Actions
- These are manual steps outside the code change
